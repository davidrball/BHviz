#functions for processing the mhd data that we've written to a csv file


import numpy


def split_dens(dens, x, y, z, deltax, deltay, deltaz):
    #take density as an input, returns particles randomly spaced in cell (w/ size deltax, deltay, deltaz)



    return 0



def sparsify(prtlist, delete_frac):
    #takes particle list and removes delete_frac of them

    return 0 